const ItemList = () => {
  return (
    <ul>
      {/* {list.map((item) => (
        <li
          key={item.id}
          style={{
            listStyle: "none",
            backgroundColor: "#EEEEEE",
            marginBottom: "10px",
            padding: 10,
          }}
        >
          <div style={{ fontSize: 20, fontWeight: 800 }}>{item.name}</div>
          <div>{item.email}</div>
        </li>
      ))} */}
    </ul>
  );
};

export default ItemList;
